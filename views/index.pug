extends layout

block content
  h1= title
  p= descr
  form(method='post', enctype='multipart/form-data' action='/convert' v-on:submit="submitForm" id="convertForm")
    input(type='file', name='imageupload')
    input(type='submit', name='uploadimage', value='Upload Image', v-on:click="clickSubmit")
